{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Jarvis overlay: custom invoke commands for the static HUD",
  "windows": ["overlay"],
  "permissions": ["core:default"]
}
