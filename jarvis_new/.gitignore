.env
.env.*
!.env.example
.DS_Store
__pycache__
.idea
KMS
.venv
.venv-wake
# Tauri/Rust build outputs (gigabytes; reproducible via scripts/build-app.sh).
# NOTE: shell/ui/ (the built Next.js export, ~18MB) IS committed on purpose:
# build-app.sh does not rebuild the frontend, so a fresh clone needs it.
shell/src-tauri/target/
.vscode
*.egg-info
.pytest_cache
.ruff_cache

# Claude Code
.claude/settings.local.json
.claude/worktrees/

# OpenAI Codex
.codex/config.local.toml

# Gemini CLI
.gemini/history/
.gemini/tmp/
.gemini/google_accounts.json
.gemini/installation_id
.gemini/oauth_creds.json

# Cursor
.cursor/chat/
.cursor/rules/*.local.mdc

# GitHub CLI
.github/personal-instructions.md
